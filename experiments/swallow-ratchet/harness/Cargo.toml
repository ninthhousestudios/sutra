[package]
name = "swr-harness"
version = "0.0.0"
edition = "2024"
publish = false

# Standalone: not a member of the sutra package.
[workspace]

[dependencies]
sutra = { path = "../../.." }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tree-sitter = "0.25"
