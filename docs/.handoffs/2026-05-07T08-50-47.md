# Handoff — sutra

## Current state

All review fixes from the third-round reviews (GLM-5.1, Qwen 3.6) are
implemented and committed. 165 tests pass, clippy has 1 pre-existing warning
(`type_complexity` on `find_dead_symbols` return type).

Two new docs added:
- `docs/review-followups.md` — deferred review items
- `docs/qartez-feature-survey.md` — feature comparison with qartez-mcp

## What to pick up next

### Near-term: Dart language gaps

See `docs/review-followups.md`. The two highest-priority items:
1. Dart else-if chain handling in cognitive complexity (currently over-penalizes)
2. Unreachable-file exclusion patterns are Rust-only (will flag Dart lib/ as dead)

Both are straightforward but need tree-sitter-dart node naming verification.

### Medium-term: new tools from qartez survey

See `docs/qartez-feature-survey.md`. Recommended order:
1. **`context`** — related files for a task. Fuses import edges, co-change,
   transitive deps. All underlying data exists; needs a scoring layer.
2. **`test_gaps`** — test coverage gap analysis. Three modes: map, gaps, suggest.
   Needs a test-file identification heuristic.
3. **`clones`** — AST shape hashing during parse. New column + grouping query.

### Backlog

- Expand complexity test coverage (see review-followups.md for the gap list)
- Add partial index on `cognitive` column for scale
- Extract `DeadSymbolRow` struct to resolve the `type_complexity` clippy warning
- Consider configurable verdict thresholds per workspace

## Context

- The `find_dead_symbols` and `find_unreachable_files` signatures changed to
  accept `path_prefix: Option<&str>` — the only caller is `src/tools/dead.rs`,
  already updated.
- `match_expression` and `switch_statement` cognitive nesting changed from
  `(true, true)` to `(true, false)`. This affects cognitive scores for any
  code with logic inside match arms. Existing DB scores will update on reparse.

---

## 2026-05-07 — qi comparison session (append)

Reviewed qi (`/home/josh/soft/qi`) and added one needs-triage task:

- **sutra/3** — `--explain` / explain flag on analysis tools (MCP-first, CLI for humans). Sutra returns scored/ranked outputs (importance in `sutra_map`, blast radius in `sutra_impact`, co-change strength, FTS fuzzy match) but the scores arrive opaque. Add explain payloads — agents need this most for trustworthy reasoning, but CLI helps humans triage. Reference surface: `qi query --explain`.

Sutra has minimal overlap with qi; only this one idea lifted. If other qi-shaped ideas surface later, the explain-mode infrastructure will already be in place.
