[package]
name = "sutra"
version = "0.2.1"
edition = "2024"
publish = false
license = "MPL-2.0"
repository = "https://github.com/ninthhousestudios/manas"
description = "Code intelligence for manas — per-workspace structural index via tree-sitter"

[[bin]]
name = "sutra"
path = "src/main.rs"

[[bin]]
name = "sutra-guard"
path = "src/bin/guard.rs"

[[bin]]
name = "hdc-spike"
path = "src/bin/hdc-spike.rs"

[[bin]]
name = "hdc-algebra"
path = "src/bin/hdc-algebra.rs"

[[bin]]
name = "hrr-semantic"
path = "src/bin/hrr-semantic.rs"

[lib]
name = "sutra"
path = "src/lib.rs"

[dependencies]
# MCP (same as sangha/chitta)
rmcp = { version = "1", features = ["server", "transport-io", "transport-streamable-http-server", "macros", "schemars"] }
schemars = "1"

# Database (same as sangha)
rusqlite = { version = "0.32", features = ["bundled"] }
parking_lot = "0.12"

# Async
tokio = { version = "1", features = ["rt-multi-thread", "macros", "signal", "sync", "net", "process"] }

# HTTP
axum = "0.8"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "json"] }
tower = "0.5"
tokio-util = "0.7"

# Tree-sitter (spike-verified: all four coexist)
tree-sitter = "0.25"
tree-sitter-rust = "0.24"
tree-sitter-dart = "0.2"
tree-sitter-c = "0.24"

# Error handling (same as sangha)
thiserror = "2"

# IDs + time + serialization
uuid = { version = "1", features = ["v7", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"

# CLI + logging + env (same as sangha)
clap = { version = "4", features = ["derive"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
dotenvy = "0.15"

# Patterns
glob = "0.3"
regex = "1"

# Hashing
blake3 = "1"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
tempfile = "3"
