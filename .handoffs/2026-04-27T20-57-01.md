# sutra — handoff

Date: 2026-04-27 (post-implementation session, Waves 1-4)

## status

Issues 1-7 complete (Waves 1-4). 3,794 LOC, 25 source files, 23 passing tests, zero clippy warnings. The project compiles, parses Rust files, resolves refs, computes rollups, serves MCP tools over HTTP/stdio, and runs a background parse scheduler.

- **Commits:** 8 on master (scaffold → db → workspace → parser → resolver+pipeline → mcp+tools → daemon+transport)
- **Plan:** `.agents/plans/2026-04-27-sutra-v01-implementation.md`

## what to pick up

### Wave 5 (parallel): Issues 8, 9, 10

**Issue 8 — 4 analysis tools + git integration.** `src/tools/{refs,calls,diff_impact,cochange}.rs` + `src/git.rs` (currently has `todo!("Issue 8")`). Behind tier gate — `sutra_tools enable: ["analysis"]` required. `src/git.rs` needs `git_diff_files()` and `git_cochange_files()` shell-outs via `tokio::process::Command`. Register the 4 analysis tools in `src/mcp.rs` tool_router block. Currently the tier gate exists in `tools_meta.rs` (AtomicBool) but no analysis tools are wired.

**Issue 9 — Dart parser.** `src/parser/dart.rs` (tree-sitter-dart, same pattern as rust.rs). Add `"dart" => dart::parse()` dispatch to `src/parser/mod.rs`. Optional per pm-010 but tree-sitter-dart compatibility was confirmed in the spike.

**Issue 10 — Integration tests + MCP contract tests + dogfooding.** `tests/workspace_lifecycle_test.rs`, `tests/mcp_contract_test.rs`, extend `tests/impact_test.rs`. Register sutra's own workspace and verify end-to-end.

### context for workers

- `src/mcp.rs` (365 lines) — SutraServer with tool_router, all 10 core tools wired. Analysis tools need to be added here with tier gate checks.
- `src/tools/mod.rs` (31 lines) — has `get_or_open_db()` helper. Add analysis tool module declarations here.
- `src/db.rs` (630+ lines) — all DB methods available. `find_refs_to_symbol()`, `find_refs_in_file()` are key for refs/calls tools.
- `src/tools/impact.rs` (98 lines) — has BFS pattern and `find_enclosing_symbol()` helper reusable for calls tool.
- `src/daemon.rs` (77 lines) — parse scheduler, stale detection. Uses tools::get_or_open_db.
- `src/main.rs` (270 lines) — fully wired serve (HTTP/stdio) and health commands.
- Sangha reference: `/home/josh/soft/manas/sangha/src/` for patterns.

## related memories

- Session summary (this session): `019dd176-523d`
- Session summary (Waves 1-3): `019dd0f5-6740`
- MCP pattern observation: `019dd175-e23e`
- Daemon transport observation: `019dd176-0507`
- Panda commit workaround: `019dd175-cb75`
- qartez guard observation: `019dd175-f56a`
