# sutra — handoff

Date: 2026-04-27 (post-implementation session, Waves 1-3)

## status

Issues 1-5 complete (Waves 1-3). 2,653 LOC, 25 source files, 18 passing tests, zero warnings. The project compiles, parses Rust files, resolves refs, and computes rollups.

- **Commits:** 6 on master (scaffold → db → workspace → parser → resolver+pipeline)
- **Plan:** `.agents/plans/2026-04-27-sutra-v01-implementation.md`

## what to pick up

### Wave 4 (parallel): Issues 6 + 7

**Issue 6 — MCP server + 10 core tools.** The big wiring issue. Implement `SutraServer` with rmcp `#[tool_router]` / `#[tool]` / `#[tool_handler]` macro pattern (see sangha's `src/mcp.rs`). 10 tools: health, map, outline, find, grep, read, impact, deps, parse (agent-triggered reparse), tools_meta (tier gate). Each tool validates workspace, gets Db, delegates to `tools::xxx::handle()`. All responses carry `as_of` + `is_stale` freshness envelope. `sutra_read` returns stale warning on deleted files. Impact tests.

**Issue 7 — Daemon + transport.** Wire `sutra serve` with HTTP (default) and `--stdio` fallback. Follow sangha's transport setup: `StreamableHttpService` on axum, PID file, signal handling, `LocalSessionManager` with extended keep-alive. Parse scheduler (background task checking stale workspaces). `smriti_client.rs` is already a stub.

### Wave 5 (parallel): Issues 8, 9, 10

**Issue 8** — 4 analysis tools (refs, calls, diff_impact, cochange) + `src/git.rs` shell-outs. Behind tier gate.
**Issue 9** — Dart parser (tree-sitter-dart, same pattern as rust.rs). Optional per pm-010.
**Issue 10** — Integration tests, MCP contract tests, dogfooding (parse sutra's own codebase).

### context for workers

- Sangha reference patterns: `/home/josh/soft/manas/sangha/src/` — `main.rs` (379 lines, CLI + transport), `mcp.rs` (242 lines, tool_router pattern), `config.rs` (95 lines), `error.rs` (158 lines)
- `src/db.rs` (672 lines) has all DB methods ready for tools to call
- `src/pipeline.rs` (521 lines) has `parse_workspace()` ready for `sutra_parse` tool
- `src/parser/mod.rs` defines all shared types (ParseResult, SymbolKind, etc.)
- `src/resolver.rs` (225 lines) — v0.1 scope, intentionally limited

## related memories

- Session summary: `019dd0f5-6740`
- Crank parallel validation: `019dd0f5-02f1`
- Resolver scope: `019dd0f5-1849`
- Tree-sitter impl details: `019dd0f4-e9b1`
- Panda heredoc workaround: `019dd0f4-d17b`
