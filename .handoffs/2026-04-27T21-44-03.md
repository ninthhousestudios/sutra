# sutra — handoff

Date: 2026-04-28 (v0.1 complete)

## status

sutra v0.1 is feature-complete and installed. All 10 issues across 5 waves done. 37 source files, 5,895 LOC, 45 passing tests, zero clippy warnings. Installed to `~/.cargo/bin/sutra`. MCP configured for Claude Code, Gemini CLI, and OpenCode (stdio transport).

- **Commits:** 12 on master (scaffold through README)
- **Binary:** `~/.cargo/bin/sutra` (release build)
- **Workspace:** sutra registered in `~/.sutra/workspaces.toml`, index at `~/.sutra/sutra/index.db`
- **Plan:** `.agents/plans/2026-04-27-sutra-v01-implementation.md`

## what to pick up

### v0.1 polish

- **Resolver improvement:** v0.1 resolver hits ~21% resolution on sutra's own codebase (plan estimated 60-70%). Heavy on generics/traits/std calls which are intentionally out of scope. Could improve by adding type_identifier matching and cross-file qualified name resolution.
- **deps tool:** Currently returns minimal results — import edges may not be resolving file IDs well. Investigate `resolved_file_id` population in the pipeline.
- **Stale worktree branches:** `worktree-agent-aae79ca2b86294d7f`, `worktree-agent-a65387efae03a8abd`, `worktree-agent-aa3709f8793c920df` — safe to delete, all merged.

### v0.1.5 candidates

- **HTTP daemon management:** systemd unit file for `sutra serve` so it runs persistently. Switch MCP configs from stdio to HTTP for shared state / parse amortization.
- **PageRank population:** Schema has nullable `pagerank` columns on files and symbols, currently always NULL. Populate from the ref graph.
- **Incremental rollup recompute:** Currently full-recompute on every parse. Only recompute changed files + depth-1 dependents (pm-006 partial implementation).

### v0.2 roadmap

- **smriti integration:** Replace on-demand parse with file-event subscription when smriti's API ships.
- **Glob import resolution:** Currently unresolved. Would improve resolution rate significantly.
- **Method dispatch on expressions:** Requires type inference, out of scope for now.

## related memories

- Session summary (this session): `019dd196-d667`
- Session summary (Wave 4): `019dd176-523d`
- Session summary (Waves 1-3): `019dd0f5-6740`
- Dogfooding metrics: `019dd196-87c0`
- Dart grammar observation: `019dd196-50af`
- db.rs bug observation: `019dd196-63f6`
