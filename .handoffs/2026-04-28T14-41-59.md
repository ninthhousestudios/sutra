# sutra — handoff

Date: 2026-04-28 (post v0.1.1 review cleanup)

## status

sutra v0.1.1 installed. Review-driven cleanup committed (da2c1fc): 15 files, +178/-237 lines. All 45 tests pass, zero warnings. Binary at `~/.cargo/bin/sutra`.

## immediate next: test coverage

Both reviews (GLM-5.1 and Qwen3.6 in `docs/reviews/`) ranked test coverage as P0. Current state: 62% of source files have zero test coverage. The new `Db` methods added this session (`resolve_symbol`, `find_enclosing_symbol`, `symbol_counts_by_file`, `all_symbols_summary`) are all untested.

Priority order:

1. **db.rs tests** — CRUD ops, FTS5 sync, cascade deletes, the new batch queries, `resolve_symbol` behavior. Use `:memory:` SQLite. This is the highest-value target because db.rs has blast radius 60.
2. **tool handler tests** — Each `handle()` is a pure function taking a `Db` reference. Easy to test with a temp DB seeded with fixture data. Start with `impact::handle` and `calls::handle` since they have the BFS logic.
3. **resolver tests** — Only 5 unit tests exist. Missing: import chain resolution, ambiguity handling (the new shortest-qualified-name tiebreaker), Dart import patterns.
4. **pipeline integration test** — Parse a small fixture directory, verify symbol/ref counts, check that rollups are populated.

The existing tests inform what's already well-covered:
- `tests/workspace_test.rs` (5 tests) — workspace TOML lifecycle is solid
- `tests/workspace_lifecycle_test.rs` (5 tests) — register/parse/query cycle, incremental reparse, delete cascade
- `tests/mcp_contract_test.rs` (10 tests) — MCP tool response shapes
- `tests/impact_test.rs` (6 tests) — impact analysis including real codebase
- Parser tests (13 tests) — Rust and Dart parsing

Gaps the tests reveal: no tests exercise the `git.rs` code paths, no tests for the `FromStr` impls added this session, and no tests verify the N+1 fixes actually return correct results (they compile, but semantic correctness isn't validated).

## next features

### v0.1.5 candidates (after test coverage)

- **PageRank population** — Schema has nullable `pagerank` columns, always NULL. Populate from the ref graph. Would improve `sutra_map` ranking quality.
- **HTTP daemon + systemd** — `sutra serve` as a persistent daemon with a systemd unit. Switch MCP from stdio to HTTP for shared state.
- **Incremental rollup recompute** — Currently full-recompute. Only recompute changed files + depth-1 dependents.
- **Resolver improvement** — v0.1 hits ~21% resolution on sutra's own codebase. Could improve with type_identifier matching and cross-file qualified name resolution.

### deferred from reviews (do later)

- **God function decomposition** — `parse_workspace` (CC=22) and `collect_symbols` (CC=28). Both reviews flagged as P0/P2 but the refactor is medium effort and doesn't fix bugs. Do after test coverage exists to catch regressions.
- **Connection pool / read-write split** — Over-engineering for current scale. Revisit if MCP latency becomes an issue with concurrent tool calls.
- **Rollups in SQL** — The in-Rust computation works. SQL version would be faster but harder to debug. Revisit at 10K+ files.
- **InsertSymbolParams struct** — Replaces 13-param `insert_symbol`. Nice-to-have, not urgent.

## housekeeping

- `src/smriti_client.rs` — Contains only a comment, needs manual `rm` (permission was denied in session). Remove it.
- Stale worktree branches: `worktree-agent-aae79ca2b86294d7f`, `worktree-agent-a65387efae03a8abd`, `worktree-agent-aa3709f8793c920df` — safe to delete.

## related memories

- Review cleanup observation: `019dd1bf-16dd`
- Session summary (this session): `019dd1c1-d9e9`
- Session summary (v0.1 complete): `019dd196-d667`
- Reviews: `docs/reviews/glm51-initial-review.md`, `docs/reviews/qwen36-initial-review.md`
